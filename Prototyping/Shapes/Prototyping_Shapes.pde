//
//Display CANVAS
//size(); //width //height
fullScreen(); //displayWidth //displayHeight
int appWidth = displayWidth;
int appHeight = displayHeight;
//rect(x, y, width, height);
//Note: the debuggger expects rectangles to have float or double type variables
//Using Ratios
float searchX = appWidth * 0.5/16;
float searchY = appHeight * 0/8;
float searchWidth = appWidth * 7.1/16;
float searchHeight = appHeight * 0.6/8;
//
rect(searchX, searchY, searchWidth, searchHeight);
//
float imageX = appWidth * 7.8/16;
float imageY = appHeight * 1.9/8;
float imageWidth = appWidth * 2/16;
float imageHeight = appHeight * 2/8;
//
rect(imageX,imageY, imageWidth, imageHeight);
//
float albumX = appWidth * 10.3/16;
float albumY = appHeight * 1.9/8;
float albumWidth = appWidth * 2/16;
float albumHeight = appHeight * 2/8;
//
rect(albumX, albumY, albumWidth, albumHeight);
//
float coverX = appWidth * 12.8/16;
float coverY = appHeight * 1.9/8;
float coverWidth = appWidth * 2/16;
float coverHeight = appHeight * 2/8;
//
rect(coverX, coverY, coverWidth, coverHeight);
//
float scrollX = appWidth * 0.5/16;
float scrollY = appHeight * 4.2/8;
float scrollWidth = appWidth * 4.5/16;
float scrollHeight = appHeight * 0.5/8;
//
rect(scrollX, scrollY, scrollWidth, scrollHeight);
//
float notifyX = appWidth * 14.3/16;
float notifyY = appHeight * 0.1/8;
float notifyWidth = appWidth * 0.5/16;
float notifyHeight = appHeight * 0.6/8;
//
rect(notifyX, notifyY, notifyWidth, notifyHeight);
//
float menuX = appWidth * 15.1/16;
float menuY = appHeight * 0.1/8;
float menuWidth = appWidth * 0.5/16;
float menuHeight = appHeight * 0.6/8;
//
rect(menuX, menuY, menuWidth, menuHeight);
//
float trackX = appWidth * 8.8/16;
float trackY = appHeight * 4.9/8;
float trackWidth = appWidth * 3.5/16;
float trackHeight = appHeight * 0.6/8;
//
rect(trackX, trackY, trackWidth, trackHeight);
//
float nameX = appWidth * 8.8/16;
float nameY = appHeight * 5.9/8;
float nameWidth = appWidth * 3.5/16;
float nameHeight = appHeight * 0.6/8;
//
rect(nameX, nameY, nameWidth, nameHeight);
//
float isX = appWidth * 8.8/16;
float isY = appHeight * 6.9/8;
float isWidth = appWidth * 3.5/16;
float isHeight = appHeight * 0.6/8;
//
rect(isX, isY, isWidth, isHeight);
//
float redX = appWidth * 14.4/16;
float redY = appHeight * 4.9/8;
float redWidth = appWidth * 0.6/16;
float redHeight = appHeight * 0.6/8;
//
rect(redX, redY, redWidth, redHeight);
//
float blueX = appWidth * 14.4/16;
float blueY = appHeight * 5.9/8;
float blueWidth = appWidth * 0.6/16;
float blueHeight = appHeight * 0.6/8;
//
rect(blueX, blueY, blueWidth, blueHeight);
//
float greenX = appWidth * 14.4/16;
float greenY = appHeight * 6.9/8;
float greenWidth = appWidth * 0.6/16;
float greenHeight = appHeight * 0.6/8;
//
rect(greenX, greenY, greenWidth, greenHeight);
//
float mcX = appWidth * 7.8/16;
float mcY = appHeight * 0.4/8;
float mcWidth = appWidth * 3.6/16;
float mcHeight = appHeight * 0.6/8;
//
rect(mcX, mcY, mcWidth, mcHeight);
//
float rnbX = appWidth * 7.8/16;
float rnbY = appHeight * 1.3/8;
float rnbWidth = appWidth * 1.5/16;
float rnbHeight = appHeight * 0.4/8;
//
rect(rnbX, rnbY, rnbWidth, rnbHeight);
//
float hipX = appWidth * 9.8/16;
float hipY = appHeight * 1.3/8;
float hipWidth = appWidth * 1.5/16;
float hipHeight = appHeight * 0.4/8;
//
rect(hipX, hipY, hipWidth, hipHeight);
//
float popX = appWidth * 11.8/16;
float popY = appHeight * 1.3/8;
float popWidth = appWidth * 1.5/16;
float popHeight = appHeight * 0.4/8;
//
rect(popX, popY, popWidth, popHeight);
//
float classicX = appWidth * 13.8/16;
float classicY = appHeight * 1.3/8;
float classicWidth = appWidth * 1.5/16;
float classicHeight = appHeight * 0.4/8;
// 
rect(classicX, classicY, classicWidth, classicHeight);
//
float exitX = appWidth * 0/16;
float exitY = appHeight * 0/8;
float exitWidth = appWidth * 0.4/16;
float exitHeight = appHeight * 0.5/8;
//
rect(exitX, exitY, exitWidth, exitHeight);
//
float homeX = appWidth * 0/16;
float homeY = appHeight * 0.7/8;
float homeWidth = appWidth * 0.4/16;
float homeHeight = appHeight * 0.5/8;
//
rect(homeX, homeY, homeWidth, homeHeight);
//
float favouriteX = appWidth * 0/16;
float favouriteY = appHeight * 1.4/8;
float favouriteWidth = appWidth * 0.4/16;
float favouriteHeight = appHeight * 0.5/8;
//
rect(favouriteX, favouriteY, favouriteWidth, favouriteHeight);
//
float lyricsX = appWidth * 0/16;
float lyricsY = appHeight * 2.1/8;
float lyricsWidth = appWidth * 0.4/16;
float lyricsHeight = appHeight * 0.5/8;
//
rect(lyricsX, lyricsY, lyricsWidth, lyricsHeight);
//
float likeX = appWidth * 6.2/16;
float likeY = appHeight * 4.2/8;
float likeWidth = appWidth * 0.5/16;
float likeHeight = appHeight * 0.5/8;
//
rect(likeX, likeY, likeWidth, likeHeight);
//
float addX = appWidth * 5.5/16;
float addY = appHeight * 4.2/8;
float addWidth = appWidth * 0.5/16;
float addHeight = appHeight * 0.5/8;
//
rect(addX, addY, addWidth, addHeight);
//
float lineX = appWidth * 0/16;
float lineY = appHeight * 5.1/8;
float lineWidth = appWidth * 7/16;
float lineHeight = appHeight * 0.5/8;
//
rect(lineX, lineY, lineWidth, lineHeight);
//
float pauseX = appWidth * 3.15/16;
float pauseY = appHeight * 5.7/8;
float pauseWidth = appWidth * 0.7/16;
float pauseHeight = appHeight * 0.7/8;
//
rect(pauseX, pauseY, pauseWidth, pauseHeight);
//
float backX = appWidth * 2.30/16;
float backY = appHeight * 5.8/8;
float backWidth = appWidth * 0.5/16;
float backHeight = appHeight * 0.5/8;
//
rect(backX, backY, backWidth, backHeight);
//
float nextX = appWidth * 4.2/16;
float nextY = appHeight * 5.8/8;
float nextWidth = appWidth * 0.5/16;
float nextHeight = appHeight * 0.5/8;
//
rect(nextX, nextY, nextWidth, nextHeight);
//
float forX = appWidth * 1.45/16;
float forY = appHeight * 5.8/8;
float forWidth = appWidth * 0.5/16;
float forHeight = appHeight * 0.5/8;
//
rect(forX, forY, forWidth, forHeight);
//
float wardX = appWidth * 5.05/16;
float wardY = appHeight * 5.8/8;
float wardWidth = appWidth * 0.5/16;
float wardHeight = appHeight * 0.5/8;
//
rect(wardX, wardY, wardWidth, wardHeight);
//
float loopX = appWidth * 0.6/16;
float loopY = appHeight * 5.8/8;
float loopWidth = appWidth * 0.5/16;
float loopHeight = appHeight * 0.5/8;
//
rect(loopX, loopY, loopWidth, loopHeight);
//
float shuffleX = appWidth * 5.9/16;
float shuffleY = appHeight * 5.8/8;
float shuffleWidth = appWidth * 0.5/16;
float shuffleHeight = appHeight * 0.5/8;
//
rect(shuffleX, shuffleY, shuffleWidth, shuffleHeight);
//
float boxX = appWidth * 0.5/16;
float boxY = appHeight * 0.8/8;
float boxWidth = appWidth * 6.3/16;
float boxHeight = appHeight * 3.3/8;
//
rect(boxX, boxY, boxWidth, boxHeight);
//
float vinylX = appWidth * 1.1/16;
float vinylY = appHeight * 0.8/8;
float vinylWidth = appWidth * 3.3/16;
float vinylHeight = appHeight * 3.3/8;
//
rect(vinylX, vinylY, vinylWidth, vinylHeight);
//  
//  
// Draw pause rectangle
rect(pauseX, pauseY, pauseWidth, pauseHeight);

// Draw pause icon inside rectangle
float barWidth = pauseWidth * 0.2;
float barHeight = pauseHeight * 0.6;
float barSpacing = pauseWidth * 0.15;
float bar1X = pauseX + pauseWidth/2 - barWidth - barSpacing/2;
float bar2X = pauseX + pauseWidth/2 + barSpacing/2;
float barY = pauseY + (pauseHeight - barHeight)/2;

rect(bar1X, barY, barWidth, barHeight);
rect(bar2X, barY, barWidth, barHeight);
//
// Draw home icon inside home rectangle
float housePad = min(homeWidth, homeHeight) * 0.18;
float houseLeft = homeX + housePad;
float houseRight = homeX + homeWidth - housePad;
float houseBottom = homeY + homeHeight - housePad;
float houseRoofPeakX = homeX + homeWidth/2;
float houseRoofPeakY = homeY + housePad;
float houseWallTop = homeY + homeHeight * 0.48; // below the roof
float houseDoorWidth = (houseRight - houseLeft) * 0.25;
float houseDoorHeight = (houseBottom - houseWallTop) * 0.43;

// Draw roof (triangle)
stroke(0);
strokeWeight(2);
noFill();
triangle(houseLeft, houseWallTop, houseRight, houseWallTop, houseRoofPeakX, houseRoofPeakY);

// Draw walls (rectangle)
rect(houseLeft, houseWallTop, houseRight - houseLeft, houseBottom - houseWallTop);

// Draw door (small rectangle, centered)
float doorX = houseRoofPeakX - houseDoorWidth/2;
float doorY = houseBottom - houseDoorHeight;
rect(doorX, doorY, houseDoorWidth, houseDoorHeight);

// Restore stroke weight
strokeWeight(1);
//
// Draw a cross (X) icon inside the exit rectangle
float crossPad = min(exitWidth, exitHeight) * 0.28; // margin from edges
float crossLeft = exitX + crossPad;
float crossRight = exitX + exitWidth - crossPad;
float crossTop = exitY + crossPad;
float crossBottom = exitY + exitHeight - crossPad;
stroke(0);
strokeWeight(3);

// Diagonal: top-left to bottom-right
line(crossLeft, crossTop, crossRight, crossBottom);
// Diagonal: bottom-left to top-right
line(crossLeft, crossBottom, crossRight, crossTop);

// Restore stroke weight
strokeWeight(1);
//
//square(nameX, nameY, nameDimension);
//line(nameX1, nameY1, nameX2, nameY2);
//triangle(nameX1, nameY1, nameX2, nameY2, nameX3, nameY3);
